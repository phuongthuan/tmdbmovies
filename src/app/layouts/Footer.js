import React, { Component } from 'react';

class Footer extends Component {
    render() {
        return (
            <footer className="ss_footer d-flex">
            </footer>
        );
    }
}

export default Footer;